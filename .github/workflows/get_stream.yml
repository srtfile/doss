      - name: Get stream URLs
        run: |
          python anime_search.py \
            --mal "${{ inputs.mal_id }}" \
            --ep  "${{ inputs.episode }}" \
            --json > result.json
          EXIT_CODE=$?
          echo "--- result.json ---"
          cat result.json
          exit $EXIT_CODE
        continue-on-error: false
